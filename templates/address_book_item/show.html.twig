{% extends 'base.html.twig' %}

{% block title %}AddressBookItem{% endblock %}

{% block body %}
    <h1>AddressBookItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ address_book_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ address_book_item.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ address_book_item.surname }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ address_book_item.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ address_book_item.email }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ address_book_item.note }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ address_book_item.createdAt ? address_book_item.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ address_book_item.updatedAt ? address_book_item.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ address_book_item.deletedAt ? address_book_item.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_address_book_item_index') }}">back to list</a>

    <a href="{{ path('app_address_book_item_edit', {'id': address_book_item.id}) }}">edit</a>

    {{ include('address_book_item/_delete_form.html.twig') }}
{% endblock %}
