<form method="post" action="{{ path('app_address_book_item_delete', {'id': address_book_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ address_book_item.id) }}">
    <button class="btn">Delete</button>
</form>
